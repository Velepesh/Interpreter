enum TokenType
{
    LEFT_PAREN, 
    RIGHT_PAREN, 
    LEFT_BRACE, 
    RIGTH_BRACE,

    COMMA, 

    MINUS, 
    PLUS, 
    MULT, 
    DIV, 
    
    SEMICOLON,
    ASSIGN,
    
    WHITESPACE,

    EQUAL, 
    EQUAL_EQUAL, 
    GREATER,
    GREATER_EQUAL,
    LESS,
    LESS_EQUAL,
    NOT_EQUAL,

    IDENTIFIER,
    NUMBER,

    AND, 
    ELSE,
    ELIF, 
    FALSE,
    TRUE,
    IF, 
    OR,
    PRINT, 
    VAR, 
    WHILE, 
    FOR,
    DO,

    JMP_VALUE,
    JMP,
    BOOLEAN,
}